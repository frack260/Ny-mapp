let bana;
attack = 'onclick="battle()"'
run = 'onclick="solis()"'
giveMoney = 'onclick="solis()"'








function easyEvent(number) {
switch (number) {
  case 1:
  var  enemyChoice = (Math.floor((Math.random()*1)+1));
  console.log(enemyChoice);

  enemyEvent(enemyChoice)

bana = new Bana(attack, run, giveMoney, story = "you jommam", image = "woods"  )


    break;
    case 2:
    var  enemyChoice = (Math.floor((Math.random()*2)+2));
    console.log(enemyChoice);
    enemyEvent(enemyChoice)

bana = new Bana(attack, run, giveMoney, story = "hello", image = "animeb"  )
      break;
      case 3:
      var  enemyChoice = (Math.floor((Math.random()*2)+4));
      console.log(enemyChoice);
      enemyEvent(enemyChoice)

  bana = new Bana(attack, run, giveMoney, story = "deep in dungeon", image = "dungeon"  )
        break;
  default:

}


  return bana;
  event(bana, enemy)


}
